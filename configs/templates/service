[Unit]
Description=ngs_{{.NameProject}} daemon
Requires=ngs_{{.NameProject}}.socket
After=network.target

[Service]
WorkingDirectory=/opt/ngs/production/{{.NameProject}}/{{.NameProject}}
ExecStart=/opt/ngs/production/{{.NameProject}}/venv/bin/gunicorn \
          --access-logfile - \
          --workers 3 \
          --bind unix:/run/ngs_{{.NameProject}}.sock \
          {{.NameProject}}.wsgi:application

[Install]
WantedBy=multi-user.target